# book-template

这是最小书项目骨架。

目录说明：

- `.webnovel/`：状态、索引、摘要、日志
- `正文/`：章节草稿和终稿
- `大纲/`：总纲、卷纲、章纲
- `设定集/`：人物、世界观、力量体系

更省事的做法还是直接用：

```bash
python -X utf8 ~/Projects/webnovel-codex/webnovel-writer/scripts/webnovel.py init \
  ~/Projects/webnovel-workspace/books/<book_slug> \
  "小说标题" \
  "修仙"
```
